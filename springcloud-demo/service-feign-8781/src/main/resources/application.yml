server:
  port: 8781
spring:
  application:
    name: service-feign
  zipkin:
    base-url: localhost:9411
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true